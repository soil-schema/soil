type: object
properties:
  name:
    type: string
  fields:
    type: object
    patternProperties:
      "^[a-zA-Z0-9_]+$":
        oneOf:
        - type: object
        - type: string
required: [name]